import { TropBienDirective } from './trop-bien.directive';

describe('TropBienDirective', () => {
  it('should create an instance', () => {
    const directive = new TropBienDirective();
    expect(directive).toBeTruthy();
  });
});
